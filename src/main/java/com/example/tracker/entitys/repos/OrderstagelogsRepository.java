package com.example.tracker.entitys.repos;

import com.example.tracker.entitys.domain.Orderstagelogs;
import org.springframework.data.jpa.repository.JpaRepository;


public interface OrderstagelogsRepository extends JpaRepository<Orderstagelogs, Long> {
}
